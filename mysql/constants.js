"use strict";

module.exports = Object.freeze({
    NUMBER_OF_QUERIES: 10000,
    NUMBER_OF_PARALLEL_RUNS: 10,
    //SQL_STRING: "SELECT * FROM broadcasts WHERE id=?;"
    SQL_STRING: "SELECT * FROM bics WHERE id=?;"
});
